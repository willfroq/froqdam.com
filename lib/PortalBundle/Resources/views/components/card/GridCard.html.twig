<a class="no-underline text-gray-900">
    <div class="bg-white rounded-[4px] p-4 hover:shadow-lg transition-shadow">
        <div class="mb-3">
            <p class="text-2xl font-semibold leading-6 text-gray-900">{{ title }}</p>
            <p class="text-xs font-semibold leading-6 text-gray-900">{{ markets }}</p>
        </div>
        <div class="flex items-center justify-center">
            <img src="{{ imageUrl }}" alt="{{ title }}" class="max-h-full object-contain">
        </div>
    </div>
</a> 